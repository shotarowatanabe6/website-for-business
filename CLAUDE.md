# CLAUDE.md

ITエンジニアとしての個人プロフィールサイト。
作業に入る前に、このファイルの前提を読む。

## このサイトの目的

新しく入社する会社の人と、懇親会で会った社外の人に、ITエンジニアとしての切り口からサイトの持ち主を紹介する。
懇親会ではQRコードでURLを渡すため、**スマートフォンでの読みやすさを最優先**にする。

読み手に持ってほしい印象は3つある。

1. Goが好き
2. 書くことが好き（部活で小説や詩を書いていた。技術文書よりも私的な文章を書くのが好き）
3. 技術に限らず広く興味を持つ

この3つのどれにも効かない変更は、入れない理由のほうが強い。
ただし、この3つをサイト上で明示するとはかぎらない。
下の「意図してそうしていること」を先に読む。

## 守ること

**本文はサイトの持ち主が書く。**
Claudeは本文を書かない。
仮テキストを置くときは、そこに何を書くかが分かる指示文にとどめ、持ち主の声を代筆しない。

**勤務先の社名を載せない。**
会社の方針を確認するまで、社名が特定できる記述を入れない。

**依存を増やさない。**
このサイトは長期間放置しても壊れないことを、機能の豊富さより優先する。
Hugoのテーマ、Hugo Modules、npmパッケージ、外部CDN、Webフォント、JavaScriptは使わない。
新しい依存を足したくなったら、まず持ち主に理由と代替案を出して判断を仰ぐ。

**サイトの作りで技術をアピールしない。**
凝ったアニメーションや構成の工夫は、上の3つの印象に効かないかぎり入れない。

**ブログ機能を作らない。**
技術ブログは既存サービスで別に運営している。
このサイトからはリンクするだけにする。

## 構成上の決定と、その理由

**静的サイトジェネレーターはHugo。**
バイナリ1つで動き、ビルドに外部のパッケージ管理系を必要としない。

**テーマは使わず、自前のレイアウトを持つ。**
公開テーマは更新が止まるとHugo本体の更新で壊れる。
1ページ5セクションという要件に対して、テーマが持つ記事一覧やタグ、ページネーションはすべて使わない。

**Hugoのバージョンを固定する。**
ローカルとCloudflare Pagesの両方で 0.152.2 を使う。
Cloudflare側は環境変数 `HUGO_VERSION` で指定する。
これを省くとCloudflare既定のHugoが使われ、ある日ビルドが壊れる。
なお `layouts/baseof.html` や `layouts/_partials/` というファイル配置はHugo 0.146以降の規則なので、バージョンを下げる場合はレイアウトの置き場所も変わる。

**本文はセクションごとに1ファイル。**
`content/sections/` に置いたMarkdownを、`layouts/home.html` が `weight` 順に連結してトップページにする。
各ファイルは `build.render: never` で単体ページとしては出力しない。
持ち主が触るのはこの4ファイルだけで済む、という状態を保つ。

**ページを増やさない。**
出力するHTMLはトップページ1枚だけにする。
趣味の詳細ページを一度作ったが、本文をトップページに収めることにしたため削除した。
分量が収まらなくなったときは、ページを足す前に、トップページに置いたままでよいかを持ち主に確認する。

**ホスティングはCloudflare Pages。**
GitHubの `main` へのpushで自動デプロイする。

## ディレクトリ

```
hugo.toml               サイト全体の設定。タイトル、説明文、OGP画像の指定
content/_index.md       ページのメタ情報のみ。本文は持たない
content/sections/       本文。持ち主が編集するのはここだけ
layouts/baseof.html     HTMLの外枠
layouts/home.html       sections を weight 順に連結する
layouts/404.html        存在しないURLに404を返すためのページ
layouts/_partials/      head（meta と OGP）、footer
assets/css/style.css    唯一のスタイルシート
static/                 og.png、icon.svg、apple-touch-icon.png
tools/make-og.sh        OGP画像とapple-touch-iconの再生成。ビルド時には動かない
```

## コマンド

```sh
hugo server            # ローカルで確認する（http://localhost:1313/）
hugo --minify          # public/ に出力する。Cloudflare Pages と同じコマンド
sh tools/make-og.sh    # hugo.toml の title か tagline を変えたときだけ実行する
```

## 意図してそうしていること

次の4点は、持ち主が意図して選んだ結果である。
不足や漏れに見えても、Claudeから指摘も提案もしない。

- 「書くこと」のセクションを置かない（持ってほしい印象の2番目だが、サイトには載せない）
- 技術ブログへのリンクを置かない
- 在籍年数や職種、時期といった経歴を本文に書かない
- `hugo.toml` の `description` を `tagline` と同じ短い一行にする

## 未決事項

以下は持ち主が決める。
Claudeが勝手に埋めない。

- 独自ドメイン名と、それに合わせた `hugo.toml` の `baseURL`
